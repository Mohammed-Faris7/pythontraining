 l=1
        high=max(piles)
        while l<=high:
            m=(l+high)//2
            total=0
            for p in piles:
                total+=(p+(m-1))//m
            if total<=h:
                high=m-1
            else:
                l=m+1
        return l
        
