#sum of subarray elements upto kth digit
arr=[1,7,3,2,8,7,3,2]
k=3
maxsum=0
for i in range(0,len(arr)-k):
    sum=0
    for j in range(i,i+k):
        sum=sum+arr[j]
    if sum>maxsum:
        maxsum=sum
print(maxsum)
#ans=18
